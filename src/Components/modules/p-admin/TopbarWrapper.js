"use client";

import { useState } from "react";
import Topbar from "./Topbar";

export default function TopbarWrapper() {
  const [isOpen, setIsOpen] = useState(false);

  // return <Topbar setIsOpen={setIsOpen} />;
}
